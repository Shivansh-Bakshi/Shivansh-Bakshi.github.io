import * as React from "react";
import Layout from "../components/layout";

const Resume = () => {
    return(
        <Layout>
            
        </Layout>
    )
}

export default Resume

export const Head = () => <title>Shivansh Bakshi | Resume</title>
